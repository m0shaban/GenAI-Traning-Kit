# 📋 دليل النشر على GitHub Pages

## الخطوات المطلوبة

### 1. تفعيل GitHub Pages

```bash
# تأكد من أنك على الفرع الصحيح
git checkout main

# أو انشئ فرع gh-pages إذا لم يكن موجوداً
git checkout --orphan gh-pages
```

### 2. إعدادات المستودع

في إعدادات المستودع على GitHub:

1. انتقل إلى `Settings` → `Pages`
2. اختر:
   - **Source**: Deploy from a branch
   - **Branch**: `main` أو `gh-pages`
   - **Folder**: `/ (root)`
3. انقر `Save`

### 3. رفع الملفات

```bash
# إضافة جميع الملفات
git add .

# إنشاء Commit
git commit -m "🚀 Deploy GenAI Training Kit v2.0"

# رفع التغييرات
git push origin main
```

### 4. التحقق من النشر

بعد الرفع مباشرة:
- انتقل إلى `Settings` → `Pages`
- يجب أن تظهر رسالة "Your site is live at: https://m0shaban.github.io/GenAI-Training-Kit/"

---

## الروابط المتاحة

| الوصف | الرابط |
|------|--------|
| **الصفحة الرئيسية** | `https://m0shaban.github.io/GenAI-Training-Kit/` |
| **الحقيبة التدريبية** | `https://m0shaban.github.io/GenAI-Training-Kit/training-package/` |
| **ملف المشروع** | `https://github.com/m0shaban/GenAI-Training-Kit` |

---

## إعدادات مخصصة (اختياري)

### Custom Domain
إذا كان لديك نطاق مخصص:

1. في `Settings` → `Pages`
2. ضع نطاقك في `Custom domain`
3. اتبع التعليمات الخاصة بـ DNS

### HTTPS
يتم تفعيل HTTPS تلقائياً!

---

## استكشاف الأخطاء

### المشكلة: الملفات لا تظهر

**الحل:**
```bash
# تأكد من أن الملفات موجودة
git status

# أعد محاولة الرفع
git push -f origin main
```

### المشكلة: الموقع لا يحمل

**الحل:**
1. امسح ذاكرة التخزين المؤقتة (Ctrl+Shift+Del)
2. جرب متصفح آخر
3. انتظر بضع دقائق (قد يحتاج الموقع وقتاً للتحديث)

### المشكلة: الأسلوب لم يتم تطبيقه

**الحل:**
```bash
# في الملفات HTML، تأكد من مسارات CSS
<link rel="stylesheet" href="css/style.css">

# وليس
<link rel="stylesheet" href="/css/style.css">
```

---

## نصائح مهمة

✅ **افعل:**
- استخدم مسارات نسبية (relative paths)
- حافظ على البنية الأساسية للملفات
- رفع الملفات بانتظام
- استخدم رسائل commit واضحة

❌ **لا تفعل:**
- لا تغير اسم الملفات الرئيسية (index.html)
- لا تستخدم مسارات مطلقة
- لا تحذف الملفات المهمة
- لا تنسى إضافة .gitignore

---

## شهادة SSL/TLS

GitHub Pages توفر شهادة HTTPS مجانية تلقائياً!

✅ آمن تماماً للمحتوى الحساس

---

## الدعم

إذا واجهت مشاكل:
1. اقرأ [GitHub Pages Documentation](https://docs.github.com/en/pages)
2. تحقق من [GitHub Status](https://www.githubstatus.com/)
3. تواصل عبر Issues في المستودع
